# oms-ocaml

CryptoCurrency OMS(Order Matching System) in OCaml.

## Quick Start

```bash
# Install dependencies
task install

# build project
task build

# Run Order Gateway
task run-order-gateway

# Run Order Server
task run-order-server
```
